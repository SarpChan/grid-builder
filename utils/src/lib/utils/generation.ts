import { GridsState, ItemState } from '@grid-builder/state';

export const generateRaw = (gridState: GridsState, areaState: ItemState) => {
  console.log(gridState, areaState);

  return '';
};

export const generateTailwind = (
  gridState: GridsState,
  areaState: ItemState
) => {
  console.log(gridState, areaState);
  return '';
};
